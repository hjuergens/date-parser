(ns date-rule-instaparse.core
  (:require [instaparse.core :as insta])
  (:gen-class))

(ns date-rule-instaparse.core
    (:gen-class))

(defn -main
    "I don't do a whole lot...yet."
    [& args]
    (println "Hello, World!"))
